package com.aciworldwide.utility.dbdependency;

import java.io.File;
import java.io.IOException;
import java.util.List;

import junit.framework.TestCase;

public class DependencyTreeParserTest{// extends TestCase {

	public void testReadFile(){
		/*TextDependencyTreeParser parser = new TextDependencyTreeParser();
		try {
			//List<String> result = parser.readFile(new File("dependency.txt"));
			Node head = parser.parse("dependency.txt");
			System.out.println(head);
			
			FarthestResolver resolver = new FarthestResolver();
			resolver.resolve(head);
			System.out.println(resolver.getDepthMap());
			System.out.println(head);
		} catch (IOException e) {
			e.printStackTrace();
		}*/
	}
}
